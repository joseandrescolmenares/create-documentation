### Configuration

The config.js file contains the configuration settings for the application, including the server port.

**Example:**

```javascript
module.exports = {
    port: process.env.PORT || 3000
  };
```